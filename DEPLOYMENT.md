# デプロイメントガイド

## 本番環境（Render）でのダミーデータ生成

本番環境でテストデータが必要な場合、以下の手順でダミーデータを生成できます。

### 1. Renderダッシュボードにアクセス

1. [Render Dashboard](https://dashboard.render.com/) にログイン
2. 対象のWebサービスを選択
3. 上部メニューから **"Shell"** タブをクリック

### 2. ダミーデータ生成コマンドを実行

Shellが開いたら、以下のコマンドを実行：

```bash
# 30件のダミープロジェクトを作成（デフォルト）
python manage.py create_dummy_data

# 50件のダミープロジェクトを作成
python manage.py create_dummy_data --count 50

# 既存データを削除してから100件作成
python manage.py create_dummy_data --count 100 --clear
```

### 3. コマンドオプション

| オプション | 説明 | デフォルト |
|----------|------|----------|
| `--count` | 作成するプロジェクト数 | 30 |
| `--clear` | 既存のプロジェクトを削除してから作成 | False |

### 4. 生成されるデータ

#### ユーザーアカウント

自動的に以下のテストユーザーが作成されます：

| ユーザー名 | パスワード | ロール | 部署 |
|----------|----------|--------|------|
| `admin` | `admin123` | 役員 | 経営管理部 |
| `tanaka` | `password123` | 営業 | 営業部 |
| `suzuki` | `password123` | 営業 | 営業部 |
| `sato` | `password123` | 営業 | 営業部 |
| `craftsman` | `password123` | 職人発注 | 施工管理部 |
| `accounting` | `password123` | 経理 | 経理部 |

#### プロジェクトデータ

- 元請名（大成建設、鹿島建設、清水建設など）
- 現場名（新宿オフィス、渋谷商業施設など）
- 施工種別（電気工事、空調工事、給排水工事など）
- 受注金額（100万〜5000万円のランダム）
- プロジェクトステータス（ネタ、施工日待ち、進行中、完工、NG）
- 工事開始日・終了日（ステータスに応じて自動設定）
- 入金・出金予定日
- 手配状況

### 5. 注意事項

- **本番環境でのデータ削除は慎重に！** `--clear` オプションは既存の全プロジェクトを削除します
- ダミーデータには `notes` フィールドに「ダミーデータ #番号」と記載されます
- ユーザーパスワードは全て `password123` または `admin123` です（本番では必ず変更してください）

### 6. デプロイ後の確認

ダミーデータ生成後、以下のページで確認できます：

- ダッシュボード: `/orders/`
- 案件一覧: `/orders/list/`
- 施工カレンダー: `/orders/calendar/`
- ガントチャート: `/orders/gantt/`
- 月次業績: `/orders/performance/monthly/`

---

## 環境変数の設定

Renderで設定が必要な環境変数：

| 変数名 | 説明 | 例 |
|-------|------|-----|
| `DATABASE_URL` | PostgreSQLデータベースURL | 自動設定 |
| `DJANGO_SECRET_KEY` | Djangoシークレットキー | ランダムな文字列 |
| `DJANGO_ALLOWED_HOSTS` | 許可するホスト名 | `your-app.onrender.com` |
| `DJANGO_DEBUG` | デバッグモード（本番はFalse） | `False` |

---

## マイグレーション

デプロイ時に自動的にマイグレーションが実行されます。
手動で実行する場合：

```bash
python manage.py migrate
```

## 静的ファイル収集

```bash
python manage.py collectstatic --noinput
```

---

**作成日**: 2025-10-27
**バージョン**: 2.3.0
