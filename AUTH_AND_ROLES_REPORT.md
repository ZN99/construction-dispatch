# 権限管理・ログイン機能レポート

## 概要
本システムには包括的な権限管理システムとログイン機能が実装されています。

---

## 1. ロール（権限）管理システム

### 📍 管理場所
`order_management/user_roles.py` で一元管理

### 定義されているロール

| ロール | コード | 説明 | 主な権限 |
|--------|--------|------|----------|
| **営業** | `UserRole.SALES` | 案件受注・顧客対応 | 案件管理、顧客対応 |
| **職人発注** | `UserRole.WORKER_DISPATCH` | 職人手配・工事管理 | 職人手配、出金予定日入力 |
| **経理** | `UserRole.ACCOUNTING` | 財務管理・入出金管理 | 入出金管理、請求書発行、会計ダッシュボード |
| **役員** | `UserRole.EXECUTIVE` | 経営管理 | 全機能アクセス、純利益閲覧、固定費閲覧 |

### 権限チェック機能

#### 基本関数
```python
has_role(user, role)          # 指定ロールを持つか
has_any_role(user, roles)     # いずれかのロールを持つか
has_all_roles(user, roles)    # すべてのロールを持つか
```

#### デコレータ
```python
@role_required(UserRole.ACCOUNTING, UserRole.EXECUTIVE)
@executive_required
@accounting_required
@worker_dispatch_required
@sales_required
```

### PermissionHelper クラス

詳細な権限チェック機能を提供：

| メソッド | 説明 | 必要なロール |
|----------|------|-------------|
| `can_view_profit()` | 純利益閲覧 | 役員のみ |
| `can_view_fixed_costs()` | 固定費閲覧 | 役員のみ |
| `can_change_payment_status()` | 出金状況変更 | 経理・役員 |
| `can_input_payment_due_date()` | 出金予定日入力 | 職人発注・役員 |
| `can_issue_invoice()` | 請求書発行 | 経理・役員 |
| `can_view_all_member_performance()` | 全メンバー営業成績閲覧 | 役員のみ |
| `can_manage_project()` | 案件管理 | 営業・役員 |
| `can_dispatch_workers()` | 職人手配 | 職人発注・役員 |

---

## 2. ログイン機能

### ✅ 本部システム用ログイン

**URL:** `/orders/login/`
**クラス:** `HeadquartersLoginView`
**機能:**
- ユーザー名・パスワード認証
- セッション管理
- スタッフ権限チェック
- リダイレクト制御
- AJAX対応

**ログイン後の処理:**
```python
- 本部スタッフ (is_staff=True) → 本部ダッシュボード
- 現場調査員 (Surveyor) → 現場調査員システム
- 権限なし → エラーメッセージ
```

### ✅ ログアウト機能

**URL:** `/orders/logout/`
**クラス:** `HeadquartersLogoutView`
**機能:**
- セッション削除
- 安全なログアウト処理
- ランディングページへリダイレクト

### ✅ 権限エラー画面

**クラス:** `PermissionDeniedView`
**機能:**
- 親切なエラーメッセージ
- 必要な権限の表示
- 適切なログインURLへの誘導
- ユーザー種別の判定

---

## 3. ユーザー管理

### 現在のユーザー状況

```
総ユーザー数: 14人
UserProfile数: 2件 ⚠️
```

### ユーザー一覧（抜粋）

| ユーザー名 | 氏名 | スタッフ | 管理者 | ロール |
|------------|------|----------|--------|--------|
| admin | - | ✅ | ✅ | なし |
| headquarters | 本部 管理者 | ✅ | ❌ | なし |
| tanaka | 太郎 田中 | ❌ | ❌ | なし |
| sato | 花子 佐藤 | ❌ | ❌ | なし |

### ⚠️ 発見された問題点

1. **UserProfileの不足**
   - 14人のユーザーに対し、UserProfileは2件のみ
   - ほとんどのユーザーにロールが割り当てられていない

2. **ロール割り当ての欠如**
   - すべてのロールで割り当て済みユーザー: 0人
   - 営業ロール: 0人
   - 職人発注ロール: 0人
   - 経理ロール: 0人
   - 役員ロール: 0人

3. **管理画面の不足**
   - ✅ Django管理画面でUserProfile管理は可能
   - ❌ ロール管理用の専用画面がない
   - ❌ 本部システム内でユーザー管理画面がない

---

## 4. 名前の登録

### ✅ 名前フィールド

Django標準の`User`モデルを使用：
- `first_name`: 名
- `last_name`: 姓
- `get_full_name()`: フルネーム取得

### 登録方法

1. **Django管理画面経由**
   - URL: `/admin/auth/user/`
   - 管理者権限で編集可能

2. **プログラムによる登録**
   ```python
   user = User.objects.create_user(
       username='username',
       first_name='太郎',
       last_name='田中',
       password='password'
   )
   ```

---

## 5. 管理画面

### ✅ Django管理画面

**URL:** `/admin/`

登録されているモデル：
- User（標準）
- UserProfile ← **ロール管理**
- Project
- CashFlowTransaction
- ForecastScenario
- Report
- その他多数

### UserProfileAdmin

**機能:**
- ユーザーとロールの紐付け
- ロールの複数選択可能
- 検索・フィルタリング機能
- 作成日時・更新日時の表示

**表示フィールド:**
```
list_display = ["user", "get_roles_display", "created_at", "updated_at"]
```

### ❌ 本部システム専用管理画面

**現状:** 未実装

**必要な機能:**
- ユーザー一覧表示
- ロール割り当て画面
- ユーザー新規作成
- パスワードリセット
- ロール別ユーザー表示

---

## 6. 推奨される改善点

### 優先度: 高

1. **全ユーザーへのUserProfile作成**
   ```python
   # 既存ユーザーにUserProfileを自動作成
   for user in User.objects.all():
       UserProfile.objects.get_or_create(user=user)
   ```

2. **適切なロール割り当て**
   - ユーザーの役割に応じてロールを割り当て
   - 最低でもheadquartersユーザーに役員ロール付与

3. **ロール管理画面の追加**
   - 本部システム内でロール管理
   - 直感的なUI/UX
   - ロール別機能一覧の表示

### 優先度: 中

4. **ユーザー登録フォームの追加**
   - 新規ユーザー作成画面
   - 初期ロール設定
   - メール通知機能

5. **権限マトリックス表示**
   - ロールごとの機能アクセス権限を可視化
   - 管理者向けドキュメント

### 優先度: 低

6. **監査ログ機能**
   - ロール変更の履歴
   - 権限エラーのログ記録

---

## 7. セキュリティ

### ✅ 実装済み

- パスワード認証
- セッション管理
- CSRF保護
- スーパーユーザーによる全権限アクセス
- ロールベースのアクセス制御
- 権限エラー時の403ページ

### 推奨事項

- パスワード複雑性の強化
- セッションタイムアウトの設定
- ログイン試行回数制限
- 2要素認証（オプション）

---

## まとめ

### ✅ 完成している機能

1. **ロール管理システム**: 4つのロールと包括的な権限チェック機能
2. **ログイン機能**: 本部用とは別に現場調査員用も実装
3. **名前登録**: Django標準のUserモデルで対応
4. **Django管理画面**: UserProfile管理が可能

### ⚠️ 改善が必要な点

1. **UserProfileの作成**: 既存ユーザーに対してProfileを作成
2. **ロールの割り当て**: 実際の業務に合わせてロールを設定
3. **本部システム内管理画面**: 専用のユーザー/ロール管理画面の追加

### 📊 システム成熟度

| 項目 | 状態 | 完成度 |
|------|------|--------|
| ロール定義 | ✅ 完成 | 100% |
| 権限チェック機能 | ✅ 完成 | 100% |
| ログイン機能 | ✅ 完成 | 100% |
| 名前登録 | ✅ 完成 | 100% |
| Django管理画面 | ✅ 完成 | 100% |
| ロールの実運用 | ⚠️ 未設定 | 15% |
| 専用管理画面 | ❌ 未実装 | 0% |

**総合評価:** 基盤は完成しているが、実運用の設定が必要

---

Generated with Claude Code
Test Date: 2025-10-31
